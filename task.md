# Optimization of Disbursement Order Logic
- [ ] Improve `bank_name` extraction to not match generic 'bank' keywords
- [ ] Improve `customer_name` extraction to skip long irrelevant sentences and account numbers
- [ ] Improve `amount` extraction robustness and ambiguity resolution
- [ ] Improve `tenure` and `rate_of_interest` matching
- [ ] Run `test_batch_do.py` to evaluate
